org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Release signing (private sideload-only tool — plaintext credentials are an
# accepted trade-off; the point is a STABLE signature so updates install cleanly).
SMSIQ_STORE_PASSWORD=smsiq-release-2026
SMSIQ_KEY_ALIAS=smsiq
SMSIQ_KEY_PASSWORD=smsiq-release-2026
